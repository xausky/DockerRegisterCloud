name: Direct Service Docker
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v1
        with:
          channel: 'stable'
      - uses: cedx/setup-dart@v1.3.0
      - run: flutter pub get
      - run: dart2native bin/drcd.dart -v -o docker/drcd
      - run: echo $PACKAGE_UPLOAD_KEY | docker login docker.pkg.github.com -u xausky --password-stdin
        env:
          PACKAGE_UPLOAD_KEY: ${{ secrets.PACKAGE_UPLOAD_KEY }}
      - run: docker build -t docker.pkg.github.com/xausky/dockerregistercloud/drcd:${{ github.ref }} docker
      - run: docker push docker.pkg.github.com/xausky/dockerregistercloud/drcd:${{ github.ref }}